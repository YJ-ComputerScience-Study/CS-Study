# 추상 클래스에서 추상 메소드는 필수적인가요?

하나 이상의 추상 메소드를 포함하는 클래스를 추상 클래스라고 합니다. 이는 앞에 `abstract`를 붙여 명시하며, 추상 클래스는 추상 메소드를 포함한다는 점을 제외하면 일반 클래스와 동일하기에 추상 클래스에서 추상 메소드는 필수적입니다. 이 추상 메소드는 자식 클래스에서 반드시 오버라이딩해야 사용할 수 있는 메소드이며, 추상 클래스에서는 구현 없이 선언부만이 존재합니다.