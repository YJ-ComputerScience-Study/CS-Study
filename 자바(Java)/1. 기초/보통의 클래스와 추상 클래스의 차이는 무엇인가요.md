# 보통의 클래스와 추상 클래스의 차이는 무엇인가요?

추상 클래스는 일반 클래스와 다르게 클래스 앞에 `abstract` 키워드가 붙습니다. 또한 클래스 내부에 `abstract` 메소드의 생성이 가능합니다. 일반 클래스 내부에는 `abstract` 메소드를 생성할 수 없습니다. 일반 클래스는 객체 생성이 가능하지만, 추상 클래스는 프로그램 설계를 위한 클래스이기에 객체 생성이 불가능합니다.